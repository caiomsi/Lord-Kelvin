/* =================================================================
   KELVIN — cable.js
   Chapter 06 — signal retardation: send a pulse down a cable of
   adjustable length and watch it smear (delay ∝ L², via
   KelvinPhysics.cableDelay / cableStepResponse — the law of squares).
   STUB — Phase 2 fills this in. Guarded no-op until then.
   ================================================================= */
(function () {
  'use strict';
  var root = document.getElementById('ch06');
  if (!root) return;
  // Phase 2 fills this in: signal-retardation interactive.
})();
