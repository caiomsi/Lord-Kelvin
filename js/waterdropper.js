/* =================================================================
   KELVIN — waterdropper.js
   Chapter 07 — the Kelvin water dropper: a positive-feedback
   electrostatic generator; voltage climbs, spark jumps.
   STUB — Phase 2 fills this in. Guarded no-op until then.
   ================================================================= */
(function () {
  'use strict';
  var root = document.getElementById('ch07');
  if (!root) return;
  // Phase 2 fills this in: water-dropper interactive.
})();
