/* =================================================================
   KELVIN — galvanometer.js
   Chapter 06 — mirror galvanometer: the optical lever amplifying a
   microamp into a readable swing; plays back a real message as
   light-spot deflections with a live transcript.
   STUB — Phase 2 fills this in. Guarded no-op until then.
   ================================================================= */
(function () {
  'use strict';
  var root = document.getElementById('ch06');
  if (!root) return;
  // Phase 2 fills this in: mirror-galvanometer playback.
})();
