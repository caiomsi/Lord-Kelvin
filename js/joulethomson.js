/* =================================================================
   KELVIN — joulethomson.js
   Chapter 05 — animated porous-plug throttling expansion; ΔT flips
   sign at the van der Waals inversion temperature
   (KelvinPhysics.jouleThomsonCoefficient / inversionTemperature).
   STUB — Phase 2 fills this in. Guarded no-op until then.
   ================================================================= */
(function () {
  'use strict';
  var root = document.getElementById('ch05');
  if (!root) return;
  // Phase 2 fills this in: Joule-Thomson throttling animation.
})();
