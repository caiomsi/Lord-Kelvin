/* =================================================================
   KELVIN — carnot.js
   Chapter 04 — Carnot engine: drag T_hot / T_cold, live
   η = 1 − Tc/Th (KelvinPhysics.carnotEfficiency), showing why a
   perfect engine would need the unreachable 0 K.
   STUB — Phase 2 fills this in. Guarded no-op until then.
   ================================================================= */
(function () {
  'use strict';
  var root = document.getElementById('ch04');
  if (!root) return;
  // Phase 2 fills this in: Carnot engine interactive.
})();
