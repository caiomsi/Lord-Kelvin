/* =================================================================
   KELVIN — gaslaw.js
   Chapter 03 — Charles's-law chart: several gas lines of different
   slope, all extrapolating to the same -273.15 °C intercept
   (KelvinPhysics.charlesVolume).
   STUB — Phase 2 fills this in. Guarded no-op until then.
   ================================================================= */
(function () {
  'use strict';
  var root = document.getElementById('ch03');
  if (!root) return;
  // Phase 2 fills this in: Charles's-law chart.
})();
