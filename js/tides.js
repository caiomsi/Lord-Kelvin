/* =================================================================
   KELVIN — tides.js
   Chapter 07 — the tide-predicting machine: drag six real tidal
   constituents (KelvinPhysics.TIDAL_CONSTITUENTS / tideSum);
   spring/neap beating emerges from M2 + S2.
   STUB — Phase 2 fills this in. Guarded no-op until then.
   ================================================================= */
(function () {
  'use strict';
  var root = document.getElementById('ch07');
  if (!root) return;
  // Phase 2 fills this in: tide-predicting-machine interactive.
})();
